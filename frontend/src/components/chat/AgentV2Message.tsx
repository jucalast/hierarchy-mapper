// ⚠️ Mantido por compatibilidade de imports.
// O conteúdo foi modularizado em `./agent/*`. Este arquivo apenas reexporta a API
// pública para não quebrar os imports existentes (`./AgentV2Message`).
export * from './agent';
